<div class="container">
  <div class="row">
    <div class="col-6">
      <%= @ad.body %>
      <%= @images.find_each do |image| %>
        <%= image_tag image.name.url %>
      <% end %>

    </div>
  </div>
</div>
